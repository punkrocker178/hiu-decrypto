import { CodeDisplayPipe } from './code-display.pipe';

describe('CodeDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new CodeDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
